## Alteração
Na tabela de usuários (`src/routes/_authenticated.administracao.tsx`), o botão da coluna "Ações" atualmente exibe ícone + texto "Editar perfil".

Remover o texto "Editar perfil", mantendo apenas o ícone `Pencil`. Preservar todas as classes e layout existentes (variant="ghost", size="sm", gap, etc.).

## Fora de escopo
- Nenhuma outra alteração de UI, layout, ou funcionalidade.
- O `aria-label` já deve estar presente para acessibilidade (ou adicionado se ausente).